# Frontend Environment Variables
# Copie este arquivo para .env.local e preencha com seus valores
# No Vite, variáveis de ambiente devem começar com VITE_

# API URL
# Para desenvolvimento local, deixe vazio (usa proxy do vite.config.ts)
# Para produção, use a URL do backend no Railway
VITE_API_URL=http://localhost:5000

# Port (opcional - Railway injeta automaticamente)
PORT=5173
