[phases.setup]
nixPkgs = ["dotnet-sdk_8"]

[phases.install]
cmds = ["cd StackHabit.API && dotnet restore"]

[phases.build]
cmds = ["cd StackHabit.API && dotnet publish --configuration Release --no-restore -o out"]

[start]
cmd = "cd StackHabit.API/out && ./StackHabit.API --urls http://0.0.0.0:$PORT"

